# サードパーティ表記

## Ageha

植毛・前髪ワークフローは `ageha` から継承しています。元コードの MIT ライセンスは `LICENSE-AGEHA-MIT` に収録しています。

## Kami4 Hair Solver

`kami4_solver/` は `kami4-hair-solver/extension/` のファイル内容を変更せずに収録しています。ネイティブ CUDA DLL を含み、GPL-3.0-or-later の対象です。元リポジトリの追跡ファイルは `vendor/kami4-hair-solver/` に保存しています。

CUDA コンポーネントと NVIDIA ドライバには、それぞれ NVIDIA のライセンス条件が適用されます。
